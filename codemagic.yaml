# =============================================================================
# codemagic.yaml - The Dread Directory
# Signed TestFlight workflow + unsigned smoke test.
# Injects iOS Info.plist permission strings (location + camera + microphone +
# photo library) automatically after Capacitor scaffolds the iOS project,
# before the build runs.
# Also writes the App.entitlements file with Sign in with Apple capability
# and patches project.pbxproj using a python one-liner to point each App
# target config at the entitlements file.
# AppIcon assets are generated from appicon_master.b64 (a base64-encoded
# 1024x1024 master PNG kept at the repo root) and copied into the
# Capacitor-generated iOS folder so home-screen and notification icons
# survive the npx cap add ios scaffolding.
# =============================================================================
workflows:
  ios-unsigned-build:
    name: iOS Unsigned Build (Pre-signing Smoke Test)
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      node: 22
      xcode: 26.2
      cocoapods: default
    scripts:
      - name: Install npm dependencies
        script: |
          npm install
      - name: Build web app (Vite)
        script: |
          npm run build
      - name: Add iOS platform (Capacitor)
        script: |
          npx cap add ios
      - name: Generate AppIcon assets and copy into iOS asset catalog
        script: |
          set -e
          if [ ! -f "appicon_master.b64" ]; then
            echo "ERROR: appicon_master.b64 not found at repo root. App icon will be generic."
            exit 1
          fi
          DEST="ios/App/App/Assets.xcassets/AppIcon.appiconset"
          mkdir -p "$DEST"
          base64 -D -i appicon_master.b64 -o "$DEST/Icon-1024.png"
          echo "Master decoded: $(ls -la $DEST/Icon-1024.png)"
          SIZES=(
            "Icon-20@2x.png:40"
            "Icon-20@3x.png:60"
            "Icon-29@2x.png:58"
            "Icon-29@3x.png:87"
            "Icon-40@2x.png:80"
            "Icon-40@3x.png:120"
            "Icon-60@2x.png:120"
            "Icon-60@3x.png:180"
            "Icon-20.png:20"
            "Icon-20@2x-ipad.png:40"
            "Icon-29.png:29"
            "Icon-29@2x-ipad.png:58"
            "Icon-40.png:40"
            "Icon-40@2x-ipad.png:80"
            "Icon-76.png:76"
            "Icon-76@2x.png:152"
            "Icon-83.5@2x.png:167"
          )
          for entry in "${SIZES[@]}"; do
            FNAME="${entry%%:*}"
            SIZE="${entry##*:}"
            sips -z "$SIZE" "$SIZE" "$DEST/Icon-1024.png" --out "$DEST/$FNAME" > /dev/null
          done
          cat > "$DEST/Contents.json" <<'JSON_EOF'
          {
            "images": [
              {"size":"20x20","idiom":"iphone","filename":"Icon-20@2x.png","scale":"2x"},
              {"size":"20x20","idiom":"iphone","filename":"Icon-20@3x.png","scale":"3x"},
              {"size":"29x29","idiom":"iphone","filename":"Icon-29@2x.png","scale":"2x"},
              {"size":"29x29","idiom":"iphone","filename":"Icon-29@3x.png","scale":"3x"},
              {"size":"40x40","idiom":"iphone","filename":"Icon-40@2x.png","scale":"2x"},
              {"size":"40x40","idiom":"iphone","filename":"Icon-40@3x.png","scale":"3x"},
              {"size":"60x60","idiom":"iphone","filename":"Icon-60@2x.png","scale":"2x"},
              {"size":"60x60","idiom":"iphone","filename":"Icon-60@3x.png","scale":"3x"},
              {"size":"20x20","idiom":"ipad","filename":"Icon-20.png","scale":"1x"},
              {"size":"20x20","idiom":"ipad","filename":"Icon-20@2x-ipad.png","scale":"2x"},
              {"size":"29x29","idiom":"ipad","filename":"Icon-29.png","scale":"1x"},
              {"size":"29x29","idiom":"ipad","filename":"Icon-29@2x-ipad.png","scale":"2x"},
              {"size":"40x40","idiom":"ipad","filename":"Icon-40.png","scale":"1x"},
              {"size":"40x40","idiom":"ipad","filename":"Icon-40@2x-ipad.png","scale":"2x"},
              {"size":"76x76","idiom":"ipad","filename":"Icon-76.png","scale":"1x"},
              {"size":"76x76","idiom":"ipad","filename":"Icon-76@2x.png","scale":"2x"},
              {"size":"83.5x83.5","idiom":"ipad","filename":"Icon-83.5@2x.png","scale":"2x"},
              {"size":"1024x1024","idiom":"ios-marketing","filename":"Icon-1024.png","scale":"1x"}
            ],
            "info": {"version": 1, "author": "xcode"}
          }
          JSON_EOF
          echo "--- AppIcon files in iOS asset catalog ---"
          ls -la "$DEST"
      - name: Inject Info.plist permission strings
        script: |
          PLIST="ios/App/App/Info.plist"
          # Force the user-facing app name to "The Dread Directory" regardless
          # of what Capacitor wrote during npx cap add ios. CFBundleDisplayName
          # is what iOS shows on the home screen and Settings; CFBundleName is
          # used internally. Patching both keeps everything consistent.
          plutil -replace CFBundleDisplayName -string "The Dread Directory" "$PLIST" || \
            plutil -insert CFBundleDisplayName -string "The Dread Directory" "$PLIST"
          plutil -replace CFBundleName -string "The Dread Directory" "$PLIST" || \
            plutil -insert CFBundleName -string "The Dread Directory" "$PLIST"
          plutil -insert NSLocationWhenInUseUsageDescription -string "The Dread Directory uses your location to show you nearby haunted, historical, and macabre sites." "$PLIST" || \
            plutil -replace NSLocationWhenInUseUsageDescription -string "The Dread Directory uses your location to show you nearby haunted, historical, and macabre sites." "$PLIST"
          plutil -insert NSLocationAlwaysAndWhenInUseUsageDescription -string "The Dread Directory needs background location access to notify you when you're near a sinister site, even when the app is closed." "$PLIST" || \
            plutil -replace NSLocationAlwaysAndWhenInUseUsageDescription -string "The Dread Directory needs background location access to notify you when you're near a sinister site, even when the app is closed." "$PLIST"
          plutil -insert NSLocationAlwaysUsageDescription -string "The Dread Directory needs background location to ping you when you're near a sinister site." "$PLIST" || \
            plutil -replace NSLocationAlwaysUsageDescription -string "The Dread Directory needs background location to ping you when you're near a sinister site." "$PLIST"
          plutil -insert NSCameraUsageDescription -string "The Dread Directory uses your camera so you can take a verification photo when submitting a new sinister site." "$PLIST" || \
            plutil -replace NSCameraUsageDescription -string "The Dread Directory uses your camera so you can take a verification photo when submitting a new sinister site." "$PLIST"
          plutil -insert NSMicrophoneUsageDescription -string "The Dread Directory uses your microphone to record audio when you capture a video for a DreadFeed post." "$PLIST" || \
            plutil -replace NSMicrophoneUsageDescription -string "The Dread Directory uses your microphone to record audio when you capture a video for a DreadFeed post." "$PLIST"
          plutil -insert NSPhotoLibraryUsageDescription -string "The Dread Directory needs photo library access so you can pick photos and videos for your DreadFeed posts." "$PLIST" || \
            plutil -replace NSPhotoLibraryUsageDescription -string "The Dread Directory needs photo library access so you can pick photos and videos for your DreadFeed posts." "$PLIST"
          plutil -insert NSPhotoLibraryAddUsageDescription -string "The Dread Directory needs permission to save photos and videos to your library." "$PLIST" || \
            plutil -replace NSPhotoLibraryAddUsageDescription -string "The Dread Directory needs permission to save photos and videos to your library." "$PLIST"
          plutil -insert NSMotionUsageDescription -string "The Dread Directory uses motion sensors to efficiently detect when you're moving, so it only checks for nearby sinister sites when needed and saves your battery." "$PLIST" || \
            plutil -replace NSMotionUsageDescription -string "The Dread Directory uses motion sensors to efficiently detect when you're moving, so it only checks for nearby sinister sites when needed and saves your battery." "$PLIST"
          plutil -insert UIBackgroundModes -array "$PLIST" 2>/dev/null || true
          plutil -insert UIBackgroundModes.0 -string location "$PLIST" 2>/dev/null || true
          plutil -insert UIBackgroundModes.1 -string fetch "$PLIST" 2>/dev/null || true
          echo "--- Final Info.plist ---"
          plutil -p "$PLIST"
      - name: Write App.entitlements (Sign in with Apple)
        script: |
          ENT="ios/App/App/App.entitlements"
          cat > "$ENT" <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>com.apple.developer.applesignin</key>
              <array>
                  <string>Default</string>
              </array>
          </dict>
          </plist>
          EOF
          echo "--- App.entitlements written ---"
          plutil -p "$ENT"
      - name: Patch Xcode project to use entitlements file
        script: |
          python3 <<'PYEOF'
          import re
          path = "ios/App/App.xcodeproj/project.pbxproj"
          with open(path, "r") as f:
              content = f.read()
          before = content.count("CODE_SIGN_ENTITLEMENTS")
          print("BEFORE: CODE_SIGN_ENTITLEMENTS count =", before)
          bundle_line = "PRODUCT_BUNDLE_IDENTIFIER = com.sinistertrivia.macabretest;"
          insert_line = "CODE_SIGN_ENTITLEMENTS = App/App.entitlements;"
          new_content = content.replace(
              bundle_line,
              bundle_line + "\n\t\t\t\t" + insert_line
          )
          with open(path, "w") as f:
              f.write(new_content)
          after = new_content.count("CODE_SIGN_ENTITLEMENTS")
          print("AFTER: CODE_SIGN_ENTITLEMENTS count =", after)
          print("Bundle ID occurrences patched:", new_content.count(bundle_line))
          PYEOF
      - name: Sync Capacitor to iOS
        script: |
          npx cap sync ios
      - name: Install CocoaPods dependencies
        script: |
          cd ios/App && pod install
      - name: Build unsigned .app for simulator (smoke test)
        script: |
          cd ios/App
          xcodebuild \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Debug \
            -sdk iphonesimulator \
            -derivedDataPath build \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO
    artifacts:
      - ios/App/build/Build/Products/**/*.app
      - ios/App/build/**/*.dSYM.zip
    publishing:
      email:
        recipients:
          - drewkrotz@gmail.com
        notify:
          success: true
          failure: true

  ios-testflight:
    name: iOS TestFlight Build (Signed)
    instance_type: mac_mini_m2
    max_build_duration: 90
    integrations:
      app_store_connect: Codemagic TestFlight
    environment:
      groups:
        - code-signing
      node: 22
      xcode: 26.2
      cocoapods: default
      vars:
        APP_STORE_APP_ID: "6765894869"
        BUNDLE_ID: "com.sinistertrivia.macabretest"
    scripts:
      - name: Install npm dependencies
        script: |
          npm install
      - name: Build web app (Vite)
        script: |
          npm run build
      - name: Add iOS platform (Capacitor)
        script: |
          npx cap add ios
      - name: Generate AppIcon assets and copy into iOS asset catalog
        script: |
          set -e
          if [ ! -f "appicon_master.b64" ]; then
            echo "ERROR: appicon_master.b64 not found at repo root. App icon will be generic."
            exit 1
          fi
          DEST="ios/App/App/Assets.xcassets/AppIcon.appiconset"
          mkdir -p "$DEST"
          base64 -D -i appicon_master.b64 -o "$DEST/Icon-1024.png"
          echo "Master decoded: $(ls -la $DEST/Icon-1024.png)"
          SIZES=(
            "Icon-20@2x.png:40"
            "Icon-20@3x.png:60"
            "Icon-29@2x.png:58"
            "Icon-29@3x.png:87"
            "Icon-40@2x.png:80"
            "Icon-40@3x.png:120"
            "Icon-60@2x.png:120"
            "Icon-60@3x.png:180"
            "Icon-20.png:20"
            "Icon-20@2x-ipad.png:40"
            "Icon-29.png:29"
            "Icon-29@2x-ipad.png:58"
            "Icon-40.png:40"
            "Icon-40@2x-ipad.png:80"
            "Icon-76.png:76"
            "Icon-76@2x.png:152"
            "Icon-83.5@2x.png:167"
          )
          for entry in "${SIZES[@]}"; do
            FNAME="${entry%%:*}"
            SIZE="${entry##*:}"
            sips -z "$SIZE" "$SIZE" "$DEST/Icon-1024.png" --out "$DEST/$FNAME" > /dev/null
          done
          cat > "$DEST/Contents.json" <<'JSON_EOF'
          {
            "images": [
              {"size":"20x20","idiom":"iphone","filename":"Icon-20@2x.png","scale":"2x"},
              {"size":"20x20","idiom":"iphone","filename":"Icon-20@3x.png","scale":"3x"},
              {"size":"29x29","idiom":"iphone","filename":"Icon-29@2x.png","scale":"2x"},
              {"size":"29x29","idiom":"iphone","filename":"Icon-29@3x.png","scale":"3x"},
              {"size":"40x40","idiom":"iphone","filename":"Icon-40@2x.png","scale":"2x"},
              {"size":"40x40","idiom":"iphone","filename":"Icon-40@3x.png","scale":"3x"},
              {"size":"60x60","idiom":"iphone","filename":"Icon-60@2x.png","scale":"2x"},
              {"size":"60x60","idiom":"iphone","filename":"Icon-60@3x.png","scale":"3x"},
              {"size":"20x20","idiom":"ipad","filename":"Icon-20.png","scale":"1x"},
              {"size":"20x20","idiom":"ipad","filename":"Icon-20@2x-ipad.png","scale":"2x"},
              {"size":"29x29","idiom":"ipad","filename":"Icon-29.png","scale":"1x"},
              {"size":"29x29","idiom":"ipad","filename":"Icon-29@2x-ipad.png","scale":"2x"},
              {"size":"40x40","idiom":"ipad","filename":"Icon-40.png","scale":"1x"},
              {"size":"40x40","idiom":"ipad","filename":"Icon-40@2x-ipad.png","scale":"2x"},
              {"size":"76x76","idiom":"ipad","filename":"Icon-76.png","scale":"1x"},
              {"size":"76x76","idiom":"ipad","filename":"Icon-76@2x.png","scale":"2x"},
              {"size":"83.5x83.5","idiom":"ipad","filename":"Icon-83.5@2x.png","scale":"2x"},
              {"size":"1024x1024","idiom":"ios-marketing","filename":"Icon-1024.png","scale":"1x"}
            ],
            "info": {"version": 1, "author": "xcode"}
          }
          JSON_EOF
          echo "--- AppIcon files in iOS asset catalog ---"
          ls -la "$DEST"
      - name: Inject Info.plist permission strings
        script: |
          PLIST="ios/App/App/Info.plist"
          # Force the user-facing app name to "The Dread Directory" regardless
          # of what Capacitor wrote during npx cap add ios. CFBundleDisplayName
          # is what iOS shows on the home screen and Settings; CFBundleName is
          # used internally. Patching both keeps everything consistent.
          plutil -replace CFBundleDisplayName -string "The Dread Directory" "$PLIST" || \
            plutil -insert CFBundleDisplayName -string "The Dread Directory" "$PLIST"
          plutil -replace CFBundleName -string "The Dread Directory" "$PLIST" || \
            plutil -insert CFBundleName -string "The Dread Directory" "$PLIST"
          plutil -insert NSLocationWhenInUseUsageDescription -string "The Dread Directory uses your location to show you nearby haunted, historical, and macabre sites." "$PLIST" || \
            plutil -replace NSLocationWhenInUseUsageDescription -string "The Dread Directory uses your location to show you nearby haunted, historical, and macabre sites." "$PLIST"
          plutil -insert NSLocationAlwaysAndWhenInUseUsageDescription -string "The Dread Directory needs background location access to notify you when you're near a sinister site, even when the app is closed." "$PLIST" || \
            plutil -replace NSLocationAlwaysAndWhenInUseUsageDescription -string "The Dread Directory needs background location access to notify you when you're near a sinister site, even when the app is closed." "$PLIST"
          plutil -insert NSLocationAlwaysUsageDescription -string "The Dread Directory needs background location to ping you when you're near a sinister site." "$PLIST" || \
            plutil -replace NSLocationAlwaysUsageDescription -string "The Dread Directory needs background location to ping you when you're near a sinister site." "$PLIST"
          plutil -insert NSCameraUsageDescription -string "The Dread Directory uses your camera so you can take a verification photo when submitting a new sinister site." "$PLIST" || \
            plutil -replace NSCameraUsageDescription -string "The Dread Directory uses your camera so you can take a verification photo when submitting a new sinister site." "$PLIST"
          plutil -insert NSMicrophoneUsageDescription -string "The Dread Directory uses your microphone to record audio when you capture a video for a DreadFeed post." "$PLIST" || \
            plutil -replace NSMicrophoneUsageDescription -string "The Dread Directory uses your microphone to record audio when you capture a video for a DreadFeed post." "$PLIST"
          plutil -insert NSPhotoLibraryUsageDescription -string "The Dread Directory needs photo library access so you can pick photos and videos for your DreadFeed posts." "$PLIST" || \
            plutil -replace NSPhotoLibraryUsageDescription -string "The Dread Directory needs photo library access so you can pick photos and videos for your DreadFeed posts." "$PLIST"
          plutil -insert NSPhotoLibraryAddUsageDescription -string "The Dread Directory needs permission to save photos and videos to your library." "$PLIST" || \
            plutil -replace NSPhotoLibraryAddUsageDescription -string "The Dread Directory needs permission to save photos and videos to your library." "$PLIST"
          plutil -insert NSMotionUsageDescription -string "The Dread Directory uses motion sensors to efficiently detect when you're moving, so it only checks for nearby sinister sites when needed and saves your battery." "$PLIST" || \
            plutil -replace NSMotionUsageDescription -string "The Dread Directory uses motion sensors to efficiently detect when you're moving, so it only checks for nearby sinister sites when needed and saves your battery." "$PLIST"
          plutil -insert UIBackgroundModes -array "$PLIST" 2>/dev/null || true
          plutil -insert UIBackgroundModes.0 -string location "$PLIST" 2>/dev/null || true
          plutil -insert UIBackgroundModes.1 -string fetch "$PLIST" 2>/dev/null || true
          echo "--- Final Info.plist ---"
          plutil -p "$PLIST"
      - name: Write App.entitlements (Sign in with Apple)
        script: |
          ENT="ios/App/App/App.entitlements"
          cat > "$ENT" <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>com.apple.developer.applesignin</key>
              <array>
                  <string>Default</string>
              </array>
          </dict>
          </plist>
          EOF
          echo "--- App.entitlements written ---"
          plutil -p "$ENT"
      - name: Patch Xcode project to use entitlements file
        script: |
          python3 <<'PYEOF'
          import re
          path = "ios/App/App.xcodeproj/project.pbxproj"
          with open(path, "r") as f:
              content = f.read()
          before = content.count("CODE_SIGN_ENTITLEMENTS")
          print("BEFORE: CODE_SIGN_ENTITLEMENTS count =", before)
          bundle_line = "PRODUCT_BUNDLE_IDENTIFIER = com.sinistertrivia.macabretest;"
          insert_line = "CODE_SIGN_ENTITLEMENTS = App/App.entitlements;"
          new_content = content.replace(
              bundle_line,
              bundle_line + "\n\t\t\t\t" + insert_line
          )
          with open(path, "w") as f:
              f.write(new_content)
          after = new_content.count("CODE_SIGN_ENTITLEMENTS")
          print("AFTER: CODE_SIGN_ENTITLEMENTS count =", after)
          print("Bundle ID occurrences patched:", new_content.count(bundle_line))
          PYEOF
      - name: Sync Capacitor to iOS
        script: |
          npx cap sync ios
      - name: Install CocoaPods dependencies
        script: |
          cd ios/App && pod install
      - name: Initialize keychain
        script: |
          keychain initialize
      - name: Fetch signing files from App Store Connect
        script: |
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --type IOS_APP_STORE \
            --create
      - name: Add certificates to keychain
        script: |
          keychain add-certificates
      - name: Set up code signing on Xcode project
        script: |
          xcode-project use-profiles
      - name: Increment build number
        script: |
          cd ios/App
          LATEST_BUILD_NUMBER=$(app-store-connect get-latest-build-number "$APP_STORE_APP_ID" || echo "1")
          NEXT_BUILD=$((LATEST_BUILD_NUMBER + 1))
          if [ "$NEXT_BUILD" -lt 41 ]; then NEXT_BUILD=41; fi
          agvtool new-marketing-version 1.10 && agvtool new-version -all "$NEXT_BUILD"
      - name: Build signed .ipa for App Store
        script: |
          cd ios/App
          xcode-project build-ipa \
            --workspace App.xcworkspace \
            --scheme App \
            --config Release
    artifacts:
      - ios/App/build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - ios/App/build/**/*.dSYM.zip
    publishing:
      email:
        recipients:
          - drewkrotz@gmail.com
        notify:
          success: true
          failure: true
      app_store_connect:
        auth: integration
        submit_to_testflight: false
        submit_to_app_store: false

  # ===========================================================================
  # ANDROID — Unsigned smoke test.
  # Mirrors ios-unsigned-build: scaffold the Android platform fresh, generate
  # icons from appicon_master.b64, patch AndroidManifest with all the
  # permissions the app needs, and build a debug APK. No signing, no upload.
  # Use this to confirm the project compiles before burning Play Console builds.
  # ===========================================================================
  android-unsigned-build:
    name: Android Unsigned Build (Pre-signing Smoke Test)
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      node: 22
      java: 21
    scripts:
      - name: Install npm dependencies
        script: |
          npm install
      - name: Build web app (Vite)
        script: |
          npm run build
      - name: Add Android platform (Capacitor)
        script: |
          npx cap add android
      - name: Generate Android app icons from appicon_master.b64
        script: |
          set -e
          if [ ! -f "appicon_master.b64" ]; then
            echo "ERROR: appicon_master.b64 not found at repo root. App icon will be generic."
            exit 1
          fi
          # Decode the 1024x1024 master PNG.
          base64 -D -i appicon_master.b64 -o /tmp/icon_master.png
          echo "Master decoded: $(ls -la /tmp/icon_master.png)"

          RES_DIR="android/app/src/main/res"
          # Android density buckets for the legacy launcher icon (ic_launcher.png)
          # and the round variant (ic_launcher_round.png). These are the fallback
          # icons used on pre-Android-8 devices; adaptive icons handle 8+.
          # NOTE: macOS ships bash 3.2 which lacks associative arrays, so we use
          # parallel arrays with a single iteration loop.
          BUCKETS=(mipmap-mdpi mipmap-hdpi mipmap-xhdpi mipmap-xxhdpi mipmap-xxxhdpi)
          SIZES=(48 72 96 144 192)
          for i in 0 1 2 3 4; do
            bucket="${BUCKETS[$i]}"
            sz="${SIZES[$i]}"
            mkdir -p "$RES_DIR/$bucket"
            # Standard square launcher icon (pre-Android-8 devices).
            sips -z "$sz" "$sz" /tmp/icon_master.png --out "$RES_DIR/$bucket/ic_launcher.png" > /dev/null
            # Round variant — Android 7.1+ uses this when the launcher requests circle icons.
            sips -z "$sz" "$sz" /tmp/icon_master.png --out "$RES_DIR/$bucket/ic_launcher_round.png" > /dev/null
            # Adaptive icon foreground layer (Android 8+). The art will sit on
            # the configured background color. Same square art for foreground —
            # Android applies the safe-zone crop automatically.
            sips -z "$sz" "$sz" /tmp/icon_master.png --out "$RES_DIR/$bucket/ic_launcher_foreground.png" > /dev/null
          done

          # Write the adaptive icon background color (Android 8+).
          # The master icon is on a black square, so background = solid black.
          mkdir -p "$RES_DIR/values"
          cat > "$RES_DIR/values/ic_launcher_background.xml" <<'XML_EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <color name="ic_launcher_background">#000000</color>
          </resources>
          XML_EOF

          # Adaptive icon XML — points the launcher at the foreground PNG and
          # the background color resource. Required for Android 8+.
          mkdir -p "$RES_DIR/mipmap-anydpi-v26"
          cat > "$RES_DIR/mipmap-anydpi-v26/ic_launcher.xml" <<'XML_EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@color/ic_launcher_background" />
              <foreground android:drawable="@mipmap/ic_launcher_foreground" />
          </adaptive-icon>
          XML_EOF
          cp "$RES_DIR/mipmap-anydpi-v26/ic_launcher.xml" "$RES_DIR/mipmap-anydpi-v26/ic_launcher_round.xml"

          echo "--- Generated Android icon resources ---"
          find "$RES_DIR" -name 'ic_launcher*' | sort
      - name: Patch AndroidManifest.xml with permissions and app name
        script: |
          set -e
          MANIFEST="android/app/src/main/AndroidManifest.xml"
          STRINGS="android/app/src/main/res/values/strings.xml"

          # App display name. Capacitor seeds strings.xml with appName from
          # capacitor.config.ts, but force it here to be explicit and to
          # survive any future drift.
          python3 <<'PYEOF'
          import re
          path = "android/app/src/main/res/values/strings.xml"
          with open(path, "r") as f:
              s = f.read()
          for key in ("app_name", "title_activity_main"):
              s = re.sub(
                  r'(<string name="' + key + r'">)[^<]*(</string>)',
                  r'\g<1>The Dread Directory\g<2>',
                  s,
              )
          with open(path, "w") as f:
              f.write(s)
          print(open(path).read())
          PYEOF

          # Inject the permission set the app needs. The geolocation plugin
          # auto-adds ACCESS_FINE_LOCATION and ACCESS_COARSE_LOCATION, but
          # background location, notifications, foreground service, motion,
          # camera, and media access all need to be declared explicitly.
          python3 <<'PYEOF'
          import re
          path = "android/app/src/main/AndroidManifest.xml"
          with open(path, "r") as f:
              s = f.read()

          permissions = [
              ('android.permission.ACCESS_FINE_LOCATION', None),
              ('android.permission.ACCESS_COARSE_LOCATION', None),
              ('android.permission.ACCESS_BACKGROUND_LOCATION', None),
              ('android.permission.POST_NOTIFICATIONS', None),
              ('android.permission.FOREGROUND_SERVICE', None),
              ('android.permission.FOREGROUND_SERVICE_LOCATION', None),
              ('android.permission.ACTIVITY_RECOGNITION', None),
              ('android.permission.RECEIVE_BOOT_COMPLETED', None),
              ('android.permission.WAKE_LOCK', None),
              ('android.permission.CAMERA', None),
              ('android.permission.RECORD_AUDIO', None),
              ('android.permission.READ_MEDIA_IMAGES', None),
              ('android.permission.READ_MEDIA_VIDEO', None),
              ('android.permission.READ_EXTERNAL_STORAGE', '32'),  # maxSdkVersion 32
              ('android.permission.WRITE_EXTERNAL_STORAGE', '28'), # maxSdkVersion 28
              ('android.permission.INTERNET', None),
              ('android.permission.ACCESS_NETWORK_STATE', None),
          ]

          # Build the <uses-permission> block.
          lines = []
          for perm, max_sdk in permissions:
              if max_sdk:
                  lines.append(f'    <uses-permission android:name="{perm}" android:maxSdkVersion="{max_sdk}" />')
              else:
                  lines.append(f'    <uses-permission android:name="{perm}" />')
          # Camera feature flag — not required, since the app gracefully degrades
          # if there's no camera (e.g. some tablets).
          lines.append('    <uses-feature android:name="android.hardware.camera" android:required="false" />')
          block = "\n".join(lines)

          # Strip any existing <uses-permission> / <uses-feature> lines Capacitor
          # may have written so we don't get duplicates.
          s = re.sub(r'\s*<uses-permission [^/]*/>', '', s)
          s = re.sub(r'\s*<uses-feature [^/]*/>', '', s)

          # Insert our block right after the opening <manifest ...> tag.
          s = re.sub(
              r'(<manifest [^>]*>)',
              r'\1\n' + block,
              s,
              count=1,
          )

          with open(path, "w") as f:
              f.write(s)
          print(open(path).read())
          PYEOF
      - name: Bump Android SDK versions to 35
        script: |
          # Capacitor 6 scaffolds android/variables.gradle with compileSdkVersion
          # and targetSdkVersion = 34. Some plugin transitive deps (androidx.work
          # 2.10.0 specifically) require API 35+. Bump both to 35 here so Gradle
          # accepts the dependency graph.
          python3 <<'PYEOF'
          import re
          path = "android/variables.gradle"
          with open(path, "r") as f:
              s = f.read()
          s = re.sub(r'compileSdkVersion\s*=\s*\d+', 'compileSdkVersion = 35', s)
          s = re.sub(r'targetSdkVersion\s*=\s*\d+', 'targetSdkVersion = 35', s)
          with open(path, "w") as f:
              f.write(s)
          print("--- variables.gradle after SDK bump ---")
          print(open(path).read())
          PYEOF
      - name: Sync Capacitor to Android
        script: |
          npx cap sync android
      - name: Build unsigned debug APK (smoke test)
        script: |
          cd android
          ./gradlew assembleDebug --no-daemon
    artifacts:
      - android/app/build/outputs/**/*.apk
      - android/app/build/outputs/**/mapping.txt
    publishing:
      email:
        recipients:
          - drewkrotz@gmail.com
        notify:
          success: true
          failure: true

  # ===========================================================================
  # ANDROID — Signed Google Play release (AAB).
  # Mirrors ios-testflight: scaffold platform, generate icons, patch manifest,
  # sign with the upload keystore, build a release AAB, and (optionally)
  # publish to a Play Console internal/closed/production track.
  #
  # Required Codemagic env vars (group: "google-play"):
  #   CM_KEYSTORE              - base64-encoded JKS upload keystore
  #   CM_KEYSTORE_PASSWORD     - keystore password
  #   CM_KEY_ALIAS             - key alias inside the keystore
  #   CM_KEY_PASSWORD          - key password
  #   GCLOUD_SERVICE_ACCOUNT_CREDENTIALS - JSON service account key for
  #                                        Google Play API (only needed if
  #                                        auto-publishing; leave blank for
  #                                        manual upload).
  # ===========================================================================
  android-googleplay:
    name: Android Google Play Build (Signed AAB)
    instance_type: mac_mini_m2
    max_build_duration: 90
    environment:
      groups:
        - google-play
      node: 22
      java: 21
      vars:
        PACKAGE_NAME: "com.sinistertrivia.macabretest"
    scripts:
      - name: Install npm dependencies
        script: |
          npm install
      - name: Build web app (Vite)
        script: |
          npm run build
      - name: Add Android platform (Capacitor)
        script: |
          npx cap add android
      - name: Generate Android app icons from appicon_master.b64
        script: |
          set -e
          if [ ! -f "appicon_master.b64" ]; then
            echo "ERROR: appicon_master.b64 not found at repo root. App icon will be generic."
            exit 1
          fi
          base64 -D -i appicon_master.b64 -o /tmp/icon_master.png
          echo "Master decoded: $(ls -la /tmp/icon_master.png)"

          RES_DIR="android/app/src/main/res"
          # NOTE: macOS bash 3.2 doesn't support associative arrays.
          BUCKETS=(mipmap-mdpi mipmap-hdpi mipmap-xhdpi mipmap-xxhdpi mipmap-xxxhdpi)
          SIZES=(48 72 96 144 192)
          for i in 0 1 2 3 4; do
            bucket="${BUCKETS[$i]}"
            sz="${SIZES[$i]}"
            mkdir -p "$RES_DIR/$bucket"
            sips -z "$sz" "$sz" /tmp/icon_master.png --out "$RES_DIR/$bucket/ic_launcher.png" > /dev/null
            sips -z "$sz" "$sz" /tmp/icon_master.png --out "$RES_DIR/$bucket/ic_launcher_round.png" > /dev/null
            sips -z "$sz" "$sz" /tmp/icon_master.png --out "$RES_DIR/$bucket/ic_launcher_foreground.png" > /dev/null
          done

          mkdir -p "$RES_DIR/values"
          cat > "$RES_DIR/values/ic_launcher_background.xml" <<'XML_EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <resources>
              <color name="ic_launcher_background">#000000</color>
          </resources>
          XML_EOF

          mkdir -p "$RES_DIR/mipmap-anydpi-v26"
          cat > "$RES_DIR/mipmap-anydpi-v26/ic_launcher.xml" <<'XML_EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
              <background android:drawable="@color/ic_launcher_background" />
              <foreground android:drawable="@mipmap/ic_launcher_foreground" />
          </adaptive-icon>
          XML_EOF
          cp "$RES_DIR/mipmap-anydpi-v26/ic_launcher.xml" "$RES_DIR/mipmap-anydpi-v26/ic_launcher_round.xml"

          # Also write a high-res Play Store icon (512x512) for upload to
          # the Play Console listing.
          sips -z 512 512 /tmp/icon_master.png --out /tmp/play_store_icon_512.png > /dev/null
          echo "--- Play Store icon ---"
          ls -la /tmp/play_store_icon_512.png
      - name: Patch AndroidManifest.xml with permissions and app name
        script: |
          set -e
          python3 <<'PYEOF'
          import re
          path = "android/app/src/main/res/values/strings.xml"
          with open(path, "r") as f:
              s = f.read()
          for key in ("app_name", "title_activity_main"):
              s = re.sub(
                  r'(<string name="' + key + r'">)[^<]*(</string>)',
                  r'\g<1>The Dread Directory\g<2>',
                  s,
              )
          with open(path, "w") as f:
              f.write(s)
          print(open(path).read())
          PYEOF

          python3 <<'PYEOF'
          import re
          path = "android/app/src/main/AndroidManifest.xml"
          with open(path, "r") as f:
              s = f.read()

          permissions = [
              ('android.permission.ACCESS_FINE_LOCATION', None),
              ('android.permission.ACCESS_COARSE_LOCATION', None),
              ('android.permission.ACCESS_BACKGROUND_LOCATION', None),
              ('android.permission.POST_NOTIFICATIONS', None),
              ('android.permission.FOREGROUND_SERVICE', None),
              ('android.permission.FOREGROUND_SERVICE_LOCATION', None),
              ('android.permission.ACTIVITY_RECOGNITION', None),
              ('android.permission.RECEIVE_BOOT_COMPLETED', None),
              ('android.permission.WAKE_LOCK', None),
              ('android.permission.CAMERA', None),
              ('android.permission.RECORD_AUDIO', None),
              ('android.permission.READ_MEDIA_IMAGES', None),
              ('android.permission.READ_MEDIA_VIDEO', None),
              ('android.permission.READ_EXTERNAL_STORAGE', '32'),
              ('android.permission.WRITE_EXTERNAL_STORAGE', '28'),
              ('android.permission.INTERNET', None),
              ('android.permission.ACCESS_NETWORK_STATE', None),
          ]

          lines = []
          for perm, max_sdk in permissions:
              if max_sdk:
                  lines.append(f'    <uses-permission android:name="{perm}" android:maxSdkVersion="{max_sdk}" />')
              else:
                  lines.append(f'    <uses-permission android:name="{perm}" />')
          lines.append('    <uses-feature android:name="android.hardware.camera" android:required="false" />')
          block = "\n".join(lines)

          s = re.sub(r'\s*<uses-permission [^/]*/>', '', s)
          s = re.sub(r'\s*<uses-feature [^/]*/>', '', s)
          s = re.sub(
              r'(<manifest [^>]*>)',
              r'\1\n' + block,
              s,
              count=1,
          )

          with open(path, "w") as f:
              f.write(s)
          print(open(path).read())
          PYEOF
      - name: Configure signing in app/build.gradle
        script: |
          set -e
          # Decode the upload keystore from the base64 env var into a real
          # JKS file inside the android folder.
          echo "$CM_KEYSTORE" | base64 -D > /tmp/upload-keystore.jks
          ls -la /tmp/upload-keystore.jks

          # Append a signingConfigs block + release config to app/build.gradle.
          # We use sed/python to patch the generated build.gradle rather than
          # checking in a custom one, so future Capacitor upgrades don't drift.
          python3 <<'PYEOF'
          import re, os
          path = "android/app/build.gradle"
          with open(path, "r") as f:
              s = f.read()

          signing_block = '''
              signingConfigs {
                  release {
                      keyAlias System.getenv("CM_KEY_ALIAS")
                      keyPassword System.getenv("CM_KEY_PASSWORD")
                      storeFile file("/tmp/upload-keystore.jks")
                      storePassword System.getenv("CM_KEYSTORE_PASSWORD")
                  }
              }
          '''

          # Insert signingConfigs right after the opening of `android {`.
          if "signingConfigs" not in s:
              s = re.sub(
                  r'(android\s*\{)',
                  r'\1\n' + signing_block,
                  s,
                  count=1,
              )

          # Point the release buildType at signingConfig.release.
          if "buildTypes" in s and "signingConfig signingConfigs.release" not in s:
              s = re.sub(
                  r'(buildTypes\s*\{[^}]*release\s*\{)',
                  r'\1\n            signingConfig signingConfigs.release',
                  s,
                  count=1,
                  flags=re.DOTALL,
              )

          with open(path, "w") as f:
              f.write(s)
          print(open(path).read())
          PYEOF
      - name: Set version code and version name
        script: |
          set -e
          # Match the iOS marketing version (1.10). Version code increments
          # from a floor of 41 to stay above any earlier internal builds,
          # using the Codemagic build number as the source of truth.
          VERSION_CODE=$(($BUILD_NUMBER + 40))
          if [ "$VERSION_CODE" -lt 41 ]; then VERSION_CODE=41; fi
          export VERSION_CODE
          python3 <<'PYEOF'
          import re, os
          version_code = os.environ["VERSION_CODE"]
          path = "android/app/build.gradle"
          with open(path, "r") as f:
              s = f.read()
          s = re.sub(r'versionCode\s+\d+', f'versionCode {version_code}', s)
          s = re.sub(r'versionName\s+"[^"]*"', 'versionName "1.10"', s)
          with open(path, "w") as f:
              f.write(s)
          print("--- versionCode/versionName patched ---")
          for line in open(path):
              if 'version' in line.lower():
                  print(line.rstrip())
          PYEOF
      - name: Bump Android SDK versions to 35
        script: |
          # Same SDK bump as the unsigned workflow; needed for androidx.work
          # 2.10.0 and other 35-targeting transitive deps.
          python3 <<'PYEOF'
          import re
          path = "android/variables.gradle"
          with open(path, "r") as f:
              s = f.read()
          s = re.sub(r'compileSdkVersion\s*=\s*\d+', 'compileSdkVersion = 35', s)
          s = re.sub(r'targetSdkVersion\s*=\s*\d+', 'targetSdkVersion = 35', s)
          with open(path, "w") as f:
              f.write(s)
          print("--- variables.gradle after SDK bump ---")
          print(open(path).read())
          PYEOF
      - name: Sync Capacitor to Android
        script: |
          npx cap sync android
      - name: Build signed release AAB
        script: |
          cd android
          ./gradlew bundleRelease --no-daemon
    artifacts:
      - android/app/build/outputs/**/*.aab
      - android/app/build/outputs/**/mapping.txt
      - /tmp/play_store_icon_512.png
    publishing:
      email:
        recipients:
          - drewkrotz@gmail.com
        notify:
          success: true
          failure: true
      # NOTE: Auto-publishing to Play Console is disabled for now. The first
      # several builds should be uploaded manually so we can verify the AAB
      # in the Play Console internal testing track before wiring up the
      # service account. To enable later, uncomment the block below and
      # add GCLOUD_SERVICE_ACCOUNT_CREDENTIALS to the google-play env group.
      #
      # google_play:
      #   credentials: $GCLOUD_SERVICE_ACCOUNT_CREDENTIALS
      #   track: internal
      #   submit_as_draft: true
